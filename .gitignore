# --- Claude Code / Spec Kit / planlama (public repo'da OLMAMALI) ---
# Sadece yazılımla ilgili docs (README, API docs vb) public olacak.
.claude/
.specify/
CLAUDE.md
GEMINI.md
AGENTS.md
ROADMAP.md
SPEC.md
PLAN.md
PLANNING.md
*.plan.md
/planning/
/specs/
/docs-internal/
/.internal/

# --- Vendored reference clients (ayrı projeler, reuse kaynağı) ---
clients/

# --- Go ---
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/bin/
/dist/
/build/
vendor/

# --- Wails ---
# frontend/dist is COMMITTED: go:embed needs it for `go build`. node_modules + the
# wails build output are not.
**/node_modules/
**/build/bin/
cmd/albion-ledger/build/
**/package.json.md5

# --- stray compiled binaries (go build without -o) ---
/probe
/albion-ledger
cmd/probe/probe
cmd/albion-ledger/albion-ledger
# code-generator scripts, when built from the repo root (they emit data/*.json)
/gen-spec-nodes
/gen-catalog
/gen-clusters

# --- Node / frontend ---
node_modules/
.pnp.*
npm-debug.log*
yarn-error.log*

# --- Local data / SQLite store ---
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
/captures/

# --- Generated capture fixtures / debug logs (not the fuzz corpus) ---
*.pcap
*.pcapng
cap.log
rep.txt
dump.txt

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
/soak
