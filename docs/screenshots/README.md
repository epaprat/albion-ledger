# Screenshots

`hero.png` is the image shown at the top of the repository `README.md` (the Flow /
silver-per-hour screen). To refresh it, replace `hero.png` with a new capture of the
current UI — ideally a populated session rather than the empty state. Keep images
reasonably sized (< ~500 KB).
